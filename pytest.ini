[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: RLS integration tests requiring a real PostgreSQL database
    slow: Slow tests skipped by default (RAG accuracy, etc.)
